server:
  port: 8086

spring:
  application:
    name: alert-service

  cloud:
    config:
      enabled: false

  # ✅ CORRECTION: Structure moderne pour MongoDB avec URI
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  mongodb:
    uri: mongodb://admin:admin@localhost:27017/risk_assessment_alerts?authSource=admin

eureka:
  client:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.riskassessment.alert: DEBUG